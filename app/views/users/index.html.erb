<table class="table">
  <tr>
    <th>名前</th>
    <th>名前</th>
  </tr>
<% @users.each do |user| %>
 <tr>
    <td><%= user.namae %></td>
    <td><%= user.email %></td>
    <td><%=link_to '詳細', user_path(user.id) %></td>
    <td><%= link_to '削除', user, method: :delete, data: { confirm: '本当に削除していいですか？' } %></td>
 </tr>
<% end %>
</table>