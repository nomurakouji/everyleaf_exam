<table class="table">
  <tr>
    <th>名前</th>
    <th>名前</th>
  </tr>
<% @users.each do |user| %>
 <tr>
    <td><%= user.namae %></td>
    <td><%= user.email %></td>
    <td><%=link_to '詳細', user_path(user.id) %></td>
 </tr>
<% end %>
</table>