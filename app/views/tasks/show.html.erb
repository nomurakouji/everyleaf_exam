<h1> タスク詳細画面 </h1>
<p>タイトル: <%= @task.name %></p>
<p>内容: <%= @task.content %></p>
<p>期限:<%= @task.deadline %></p>
<p>優先順位:<%= @task.priority %></p>
<p>状態:<%= @task.condition%> </p>
<p>タグ:
  <% @post_tags.each do |tag| %>
    <%=tag.name%>
  <% end %>
</p>
<p><%= link_to "タスク一覧に戻る", tasks_path %>